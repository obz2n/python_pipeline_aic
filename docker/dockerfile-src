FROM python:3.11-slim

# place ourselves directly in the source tree so that a bare import
# of api_client will resolve when extractor.py is executed
WORKDIR /app/src

COPY requirements.txt ../
RUN pip install --no-cache-dir -r ../requirements.txt

# copy the python modules into the current working directory
COPY src/ ./

CMD ["python", "extractor.py"]