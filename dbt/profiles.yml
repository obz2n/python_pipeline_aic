aic_pipeline:
  target: dev
  outputs:
    dev:
      type: duckdb
      # DUCKDB_PATH deve estar definido no .env
      # Docker:  DUCKDB_PATH=/app/data/warehouse/aic.duckdb
      # Local:   DUCKDB_PATH=data/warehouse/aic.duckdb
      path: "{{ env_var('DUCKDB_PATH') }}"
      threads: 4